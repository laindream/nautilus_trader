# 基于Actor的信号消息示例

此示例演示了 NautilusTrader 中使用 *基于actor的信号* 的最简单消息形式。
它展示了如何使用 *基于字符串的信号* 在组件之间实现轻量级通知。

## 您将学到什么

- 如何使用信号进行简单通知（本例中为价格极值）。
- 如何发布带有单个字符串值的信号。
- 如何订阅信号并在 `on_signal` 回调中处理它们。

## 实现亮点

- 使用 `SimpleNamespace` 作为信号名称常量。
- 展示信号发布和订阅。
- 演示在 `on_signal` 中使用模式匹配的信号处理。
